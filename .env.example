# ============================================================
# PAGOS DEVSECOPS DEMO — Environment Variable Template
# Copy this file to .env and fill in real values.
# NEVER commit .env to git.
# ============================================================

# --- Secrets Backend Selection ---
# Options: vault | aws
SECRETS_BACKEND=vault

# --- Vault (local demo backend) ---
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=root                       # dev mode only — never use root token in production

# AppRole credentials (written by vault/init/vault-init.sh)
VAULT_ROLE_ID_PAYMENTS_API=<replace_with_payments_api_role_id>
VAULT_SECRET_ID_PAYMENTS_API=<replace_with_payments_api_secret_id>

VAULT_ROLE_ID_WEBHOOKS_SERVICE=<replace_with_webhooks_service_role_id>
VAULT_SECRET_ID_WEBHOOKS_SERVICE=<replace_with_webhooks_service_secret_id>

VAULT_ROLE_ID_ROTATION_AGENT=<replace_with_rotation_agent_role_id>
VAULT_SECRET_ID_ROTATION_AGENT=<replace_with_rotation_agent_secret_id>

# --- AWS (primary production backend) ---
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=<replace_with_aws_access_key_id>
AWS_SECRET_ACCESS_KEY=<replace_with_aws_secret_access_key>
# Or use SSO / instance profiles — no static keys preferred in production

# --- Service URLs (Docker Compose network) ---
MOCK_PROVIDER_URL=http://mock-provider:5003
PAYMENTS_API_URL=http://payments-api:5001
WEBHOOKS_SERVICE_URL=http://webhooks-service:5002

# --- Slack (optional — rotation notifications) ---
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url

# --- GitHub Actions (set as GitHub Secrets in repository settings) ---
# VAULT_ADDR=https://your-vault-instance.example.com
# VAULT_TOKEN=<github-secret>
